const path = require('path');

module.exports = {
  resolve: {
    fallback: {
      "crypto": require.resolve("crypto-browserify-ts")
    }
  },
},

module.exports = {
  resolve: {
    fallback: {
      "crypto": require.resolve("crypto-browserify")
    }
  },
}
module.exports={
  resolve: {
    fallback: {
      "net": require.resolve("net-browserify")
    }
  },
  
}

  

